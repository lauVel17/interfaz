/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConfirmEliminarComponent } from './confirm-eliminar.component';

describe('ConfirmEliminarComponent', () => {
  let component: ConfirmEliminarComponent;
  let fixture: ComponentFixture<ConfirmEliminarComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [ConfirmEliminarComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ConfirmEliminarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */